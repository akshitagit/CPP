#include <stdio.h>

int main()
{
    int length;
    scanf("%d", &length);
    int arr[length];
    int sum=0;
    for(int i=0; i<length; i++)
        scanf("%d", &arr[i]);
    for(int i=0; i<length; i++)
        sum+=arr[i];
    printf("%d", sum);

    return 0;
}
